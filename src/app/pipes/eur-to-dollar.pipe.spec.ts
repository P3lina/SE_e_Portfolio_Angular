import { EurToDollarPipe } from './eur-to-dollar.pipe';

describe('EurToDollarPipe', () => {
  it('create an instance', () => {
    const pipe = new EurToDollarPipe();
    expect(pipe).toBeTruthy();
  });
});
