import { StringArrayToStringPipe } from './string-array-to-string.pipe';

describe('StringArrayToMultLineStringPipe', () => {
  it('create an instance', () => {
    const pipe = new StringArrayToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
